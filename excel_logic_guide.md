# エクセルによる実習可能性シミュレーション構築ガイド

このガイドでは、Webシミュレーターのロジックをご自身のエクセルで再現し、計算過程を検証するための手順を説明します。
Excelでは「個々の学生の動き」をシミュレートするのは複雑ですが、「**全体のキャパシティ（供給） vs 必要時間（需要）**」のバランス計算は簡単に行えます。

---

## 1. シート構成

Excelブックに以下の2つのシートを作成することをお勧めします。

1.  **Parameters (設定シート)**: 前提条件を入力します。
2.  **Calculation (計算シート)**: 日別のキャパシティを計算します。

---

## 2. Parameter シート (設定入力)

A列に項目名、B列に値を入力します。

| セル | 項目名 (A列) | 値の例 (B列) | 説明 |
| :--- | :--- | :--- | :--- |
| **A1** | 学生数 | 100 | 実習を行う学生の総数 |
| **A2** | 1人あたり必要時間 | 21 | 全体で完了すべき時間 |
| **A3** | 1回あたりの実習時間 | 3 | "1日の実習時間/人"の設定 |
| **A4** | 同時実習最大人数 | 5 | クリニックの座席数/ユニット数 |
| **A5** | **必要総セッション数** | `=B1 * CEILING(B2/B3, 1)` | 全員が終わるのに必要な「枠」の総数 (※1) |

*(※1) 21時間を3時間ずつ行う場合、1人7回必要。100人で700枠必要。*

---

## 3. Calculation シート (日別計算)

このシートで1日ごとの供給量を計算し、合計します。

### ヘッダー (1行目)
以下のように列を作成してください。

| 列 | 項目名 | 説明 |
| :--- | :--- | :--- |
| **A** | 日付 | 2026/4/1 からオートフィル |
| **B** | 曜日 | `=TEXT(A2, "aaa")` |
| **C** | 開院時間 (時間) | その日の開院時間数 (例: 12時間) |
| **D** | 授業ブロック | 授業で潰れる時間があるか (TRUE/FALSE または 時間数) |
| **E** | 有効開院時間 | 授業や休憩を除いた、純粋に使える時間 |
| **F** | 1席あたり回転数 | `=FLOOR(E2 / Parameters!$B$3, 1)` (※2) |
| **G** | 日次最大キャパ | `=F2 * Parameters!$B$4` (※3) |

### 計算式の解説 (詳細)

#### (※2) 1席あたり回転数 (F列)
「その日、1つのユニットで何人の学生を回せるか？」を計算します。
*   例えば、有効開院時間が **8時間** で、1回の実習が **3時間** の場合：
*   `=FLOOR(8 / 3, 1)` = **2回** (余り2時間は使えないため切り捨て)
*   Webシミュレーターが厳密に行っている計算を簡易化したものです。

#### (※3) 日次最大キャパ (G列)
「その日、クリニック全体で延べ何人の学生を受け入れられるか？」
*   `回転数` × `同時実習最大人数 (5人)`
*   例: 2回転 × 5席 = **10枠** (10人分)

---

## 4. 判定 (Feasibility Check)

Calculationシートの最後に合計を出します。

| 集計項目 | 計算式 | 判定基準 |
| :--- | :--- | :--- |
| **供給能力合計 (Supply)** | `=SUM(G:G)` | シミュレーション期間中の全枠数 |
| **必要需要合計 (Demand)** | `=Parameters!$B$5` | 学生全員を終わらせるのに必要な枠数 |
| **達成率** | `=Supply / Demand` | **100%未満なら達成不可能** |

---

## 5. Webシミュレーターとの違い

このExcel計算は「**需要と供給の総量バランス**」を見るものです。
Webシミュレーターはさらに以下の詳細な制約を考慮しています：

1.  **個別のスケジュール**: 「Aさんは月曜不可、Bさんは火曜不可」といった個別事情。Excelの合計計算では「誰でもいつでも来れる」前提になります。
2.  **ラストピース問題**: 期間の最後の方で、残り1時間の学生しか残っていないのに3時間枠を使ってしまう等、効率の低下（Web版はこれをシミュレートします）。

したがって、**Excelで「100%ギリギリ」の結果が出た場合、実際（Webシミュレーター）には「達成不可」になる可能性が高い**です。Excelでは **120%以上** の余裕があることを目安にしてください。
